[project]
name = "notification"
version = "0.1.0"
description = "Notification microservice"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "pika>=1.4.4",
]

[tool.uv]
package = false
